# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'drunk' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for drunk
  pod 'SwaggerClient', :path => './lib/SwaggerClient'

  target 'drunkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'drunkUITests' do
    # Pods for testing
  end

end
